<div class="container mt-4">
  <div class="card">
    <div class="card-header">
      <h2>Detalles de la Reserva #<%= @reserva.id %></h2>
    </div>
    <div class="card-body">
      <div class="row">
        <div class="col-md-6">
          <p><strong>ID:</strong> <%= @reserva.id %></p>
          <p><strong>Fecha:</strong> <%= @reserva.fecha %></p>
          <p><strong>Estado:</strong> 
            <span class="badge 
              <%= @reserva.estado == 'pendiente' ? 'bg-warning' : 
                  @reserva.estado == 'confirmada' ? 'bg-success' : 'bg-danger' %>">
              <%= @reserva.estado %>
            </span>
          </p>
        </div>
        <div class="col-md-6">
          <p><strong>Usuario ID:</strong> <%= @reserva.usuario_id %></p>
          <p><strong>Opción:</strong> <%= @reserva.opcion.nombre %></p>
          <p><strong>Sección:</strong> <%= @reserva.opcion.seccion.nombre %></p>
          <p><strong>Resort:</strong> <%= @reserva.opcion.seccion.resort.nombre %></p>
        </div>
      </div>
      
      <div class="mt-4">
        <%= link_to "✏️ Editar", edit_reserva_path(@reserva), class: "btn btn-warning" %>
        <%= link_to "📋 Volver al Listado", reservas_path, class: "btn btn-secondary" %>
        <%= link_to "🏠 Inicio", root_path, class: "btn btn-outline-primary" %>
      </div>
    </div>
  </div>
</div>